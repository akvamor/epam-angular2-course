import { CourseOrderPipe } from './course-order.pipe';

describe('CourseOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new CourseOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
